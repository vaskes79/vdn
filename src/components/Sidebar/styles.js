const styles = theme => ({
  root: {
    background: theme.palette.background.paper,
    position: 'absolute',
  },
  paper: {
    width: '30%',
    maxWidth: 650,
    minWidth: 200,
  },
});

export default styles;
