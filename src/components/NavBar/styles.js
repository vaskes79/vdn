const styles = theme => ({
  root: {
    flexGrow: 1,
    background: theme.palette.background.paper,
  },
  menuButton: {
    marginRight: theme.spacing(2),
  },
  fab: {},
});

export default styles;
